<div class="col-sm-12">
	<div class="order-box">
		<h3>Chi tiết đơn hàng</h3>
		<span>Mã đơn hàng: {{info.id}}</span>
		<span>Trạng thái: {{info.status}}</span>
		<hr>
		<form method="POST" action="/order/create">
		<input type="hidden" name="_csrf" value="{{csrfToken}}">
			<div class="row">
				<div class="col-sm-6">
					<div class="form-group">
						<label for="buyer-name">Người đặt</label>
						<input id="buyer-name" class="form-control-plaintext" type="text" value="{{info.user_id}}" readonly>
					</div>
					<div class="form-group">
						<label for="buyer-name">Người nhận</label>
						<input id="buyer-name" class="form-control-plaintext" type="text" name="receiver_name" value="{{info.receiver_name}}" required>
					</div>
					<div class="form-group">
						<label for="buyer-name">SĐT Người nhận</label>
						<input id="buyer-name" class="form-control-plaintext" type="text" name="receiver_phone" value="{{info.receiver_phone}}" required>
					</div>
					<div class="form-group">
						<label for="buyer-name">Email Người nhận</label>
						<input id="buyer-name" class="form-control-plaintext" type="text" name="receiver_email" value="{{info.receiver_email}}" required>
					</div>
					<div class="form-group">
						<label for="buyer-name">Địa chỉ nhận hàng</label>
						<input id="buyer-name" class="form-control-plaintext" type="text" name="receiver_address" value="{{info.receiver_address}}" required>
					</div>
				</div>
				<div class="col-sm-6">
					<div class="form-group">
						<label for="buyer-name">Chi tiết đơn hàng</label>
						<table class="table">
							<thead class="header">
								<tr>
									<th>Hình ảnh</th>
									<th>Tên sản phẩm</th>
									<th>Số lượng</th>
									<th>Giá</th>
									<th>Tổng giá</th>
								</tr>
							</thead>
							<tbody>
								{{#each info.items}}
								<tr>
									<td class="item-image"><img src="{{this.images}}"></td>
									<td>{{this.name}}</td>
									<td>{{this.number}}</td>
									<td>{{this.price}}</td>
									<td>{{this.totalPrice}}</td>
								</tr>
								{{/each}}
							</tbody>
						</table>
					</div>
					<div class="form-group">
						<label>Tổng tiền</label>
						<input type="text" class="form-control-plaintext" value="{{info.cost}}">
					</div>
				</div>
			</div>
		</form>
	</div>
</div>